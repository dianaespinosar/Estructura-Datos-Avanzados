//Lectura de archivo
import sys

file = open(sys.argv[1])
n = int(file.readlines()
print ("Son %d parejas", % n/2)
for x in (0, n)
	a = int(file.read())
	b = int(file.read())
	print ("La suma de la pareja ", x, " es ", (a+b))
	print ("La suma de la pareja %d es %d", %(x, (a+b)
file.
